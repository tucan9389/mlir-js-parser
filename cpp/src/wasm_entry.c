// Dummy C file to produce a main-less Emscripten module exposing C symbols from parser.cc
// We do not define main; the module acts as a library.
#include <stdint.h>

// Nothing here; all exports come from parser.cc
